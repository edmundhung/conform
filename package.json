{
	"private": true,
	"name": "conform",
	"license": "MIT",
	"scripts": {
		"dev": "pnpm --recursive --parallel --stream run dev",
		"guide": "pnpm --filter ./guide",
		"playground": "pnpm --filter ./playground",
		"prepare": "husky",
		"test:api": "pnpm playground test:api",
		"test:e2e": "pnpm playground test:e2e",
		"test": "pnpm run /test:.*/"
	},
	"devDependencies": {
		"@conform-to/dom": "workspace:*",
		"@conform-to/react": "workspace:*",
		"@conform-to/yup": "workspace:*",
		"@conform-to/zod": "workspace:*",
		"husky": "9.0.11",
		"lint-staged": "15.2.2",
		"prettier": "3.2.5"
	},
	"engines": {
		"node": ">=20"
	}
}
