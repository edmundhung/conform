name: Validate
on:
  workflow_dispatch:
    inputs:
      preview:
        description: 'Commit SHA / PR Number'
        type: string
        required: false
        default: ''
      typescript:
        description: 'JSON array of TypeScript versions, ranges, or tags (e.g. ["5.8", "7"])'
        type: string
        required: false
        default: ''
  workflow_call:
    inputs:
      preview:
        description: 'Commit SHA / PR Number'
        type: string
        required: false
        default: ''
      typescript:
        description: 'JSON array of TypeScript versions, ranges, or tags (e.g. ["5.8", "7"])'
        type: string
        required: false
        default: ''

permissions:
  contents: read

jobs:
  examples:
    name: Examples
    if: inputs.preview != ''
    timeout-minutes: 15
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repo
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      - name: Setup pnpm and Node
        uses: pnpm/setup@4700d737c3d7a2e7199f3d42a920f0bf7f34e411 # v2.0.1
        with:
          runtime: node@22
          cache: true
      - name: Build packages
        run: pnpm build
      - name: Install pre-release
        env:
          PNPM_CONFIG_BLOCK_EXOTIC_SUBDEPS: 'false'
        run: >
          pnpm --filter './examples/*' add
          https://pkg.pr.new/@conform-to/react@${{ inputs.preview }}
          https://pkg.pr.new/@conform-to/zod@${{ inputs.preview }}
      - name: Install Playwright Browsers
        run: pnpm exec playwright install --with-deps
      - name: Run tests
        run: pnpm --filter './examples/*' run --if-present test

  typecheck:
    name: Typecheck — TS ${{ matrix.typescript }}
    if: inputs.typescript != ''
    timeout-minutes: 10
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        typescript: ${{ fromJSON(inputs.typescript) }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      - name: Setup pnpm and Node
        uses: pnpm/setup@4700d737c3d7a2e7199f3d42a920f0bf7f34e411 # v2.0.1
        with:
          runtime: node@22
          cache: true
      - name: Build packages
        if: inputs.preview == ''
        run: pnpm build
      - name: Install pre-release
        if: inputs.preview != ''
        env:
          PNPM_CONFIG_BLOCK_EXOTIC_SUBDEPS: 'false'
        run: >
          pnpm --filter './examples/*' add
          https://pkg.pr.new/@conform-to/react@${{ inputs.preview }}
          https://pkg.pr.new/@conform-to/zod@${{ inputs.preview }}
      - name: Generate framework types
        run: |
          pnpm --filter nextjs-example exec next typegen
          pnpm --filter react-router-example exec react-router typegen
      - name: Typecheck examples
        # Astro supports TypeScript 5 only and the legacy Remix example will be removed.
        env:
          TYPESCRIPT_SPEC: ${{ matrix.typescript }}
        run: |
          pnpm --package="typescript@$TYPESCRIPT_SPEC" dlx tsc --version
          pnpm --package="typescript@$TYPESCRIPT_SPEC" dlx tsc \
            --build \
            examples/chakra-ui \
            examples/headless-ui \
            examples/material-ui \
            examples/nextjs \
            examples/radix-ui \
            examples/react-aria \
            examples/react-router \
            examples/react-spa \
            examples/shadcn-ui \
            --pretty false

  epicstack:
    name: Epic Stack
    if: inputs.preview != ''
    timeout-minutes: 10
    runs-on: ubuntu-22.04
    steps:
      - name: ⎔ Setup node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22

      - name: Bootstrap project
        run: SKIP_DEPLOYMENT=true npx --yes epicli@latest new ./

      - name: Install pre-release
        run: |
          npm i https://pkg.pr.new/@conform-to/react@${{ inputs.preview }} https://pkg.pr.new/@conform-to/zod@${{ inputs.preview }}

      - name: Copy test env vars
        run: cp .env.example .env

      - name: Setup Database
        run: npx prisma migrate deploy && npx prisma generate --sql

      - name: Cache Database
        id: db-cache
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: prisma/data.db
          key: db-cache-schema_${{ hashFiles('./prisma/schema.prisma')
            }}-migrations_${{ hashFiles('./prisma/migrations/*/migration.sql')
            }}

      - name: Seed Database
        if: steps.db-cache.outputs.cache-hit != 'true'
        run: npx prisma migrate reset --force

      - name: Build
        run: npm run build

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run typecheck --if-present

      - name: Run vitest
        run: npm run test -- --coverage

      - name: Playwright tests
        run: npx playwright test

      - name: Upload report
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
